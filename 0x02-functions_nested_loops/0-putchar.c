#include <unistd.h>

/**
 * _putchar - writes the characher c to stdi
 * @c: The character to input
 * Return: On Success 0
 * On error, -1 is returened, and error
 */
int _putchar(char c)
{
	putchar("_");
	putchar("putchar");
	putchar("\n");

	return (0);
}
